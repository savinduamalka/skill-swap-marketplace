// This file is intentionally kept minimal to avoid TypeScript errors
// from the 'livekit-client' package when not used.

export function useLiveKit() {
  // Placeholder hook: not used in the current implementation.
  // Returning an empty object to satisfy potential imports if any.
  return {} as any;
}
